{
  "name": "@soldecoder-monitor/package-utils",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.876.0",
    "@aws-sdk/lib-dynamodb": "^3.876.0",
    "@soldecoder-monitor/config-env": "*",
    "@soldecoder-monitor/logger": "*"
  },
  "devDependencies": {
    "@soldecoder-monitor/typescript-config": "*",
    "@types/node": "^20.19.11",
    "typescript": "^5.6.3"
  }
}
